services:
  vector-db:
    image: qdrant/qdrant
    ports:
      - 6333:6333
    volumes:
      - qdrant_data:/qdrant/storage
    networks:
      - shared-net

volumes:
  qdrant_data:

networks:
  shared-net:
    external: true
    name: my-shared-network
  